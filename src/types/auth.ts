export type AuthCredentials = {
    login: string;
    password: string;
};

export type ClientUser = {
    name: string;
    avatarUrl: string;
    isPro: boolean;
    email: string;
    token: string;
}
